# nvim

Tracked default Neovim config directory.

This intentionally has no `init.lua`, so the plain `nvim` command keeps normal
Neovim behavior while the config directory is still synced across machines.
